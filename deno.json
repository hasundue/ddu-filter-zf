{
  "tasks": {
    "build": "deno run -A build.ts",
    "download": "deno run -A download.ts",
    "test": "deno test -A --unstable",
    "dev": "deno fmt && deno lint && deno task test"
  },
  "lock": false
}
